import React from 'react';

const Andi = (props) => {
  return (
    <>
      <p>Name: {props.name}</p>
      <p>Role: {props.role}</p>
      <p>And Title: {props.title}</p>
    </>
  );
};

export default Andi;
